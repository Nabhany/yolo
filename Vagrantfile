Vagrant.configure("2") do |config|
    config.vm.box = "ubuntu/focal64" # Ubuntu 20.04 LTS (Focal Fossa)
  
    config.vm.provider "virtualbox" do |vb|
      vb.memory = "1024" # Set VM memory to 1GB
      vb.cpus = "2"     # Set VM CPU count to 2
    end
  end
  